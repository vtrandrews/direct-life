import { LoadingWrapper, LogoLoading } from "../styles/styles-modules";

export const LoadingLogo = () => {
  return (
    <LoadingWrapper>
      <LogoLoading />
    </LoadingWrapper>
  );
};

export default LoadingLogo;
